package EnumPack;

public enum DayOfWeek {
    SUNDAY(), MONDAY(), TUESDAY(), WEDNESDAY(), THURSDAY(), FRIDAY(), SATURDAY();
    /*
    //how to use?
    private int number;

    DayOfWeek(int number) {
        this.number = number;
    }
     */
}
